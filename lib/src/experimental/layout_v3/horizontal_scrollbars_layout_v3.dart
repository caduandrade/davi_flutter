import 'package:easy_table/src/experimental/table_scrollbar.dart';
import 'package:flutter/material.dart';

class HorizontalScrollbarsLayoutV3 extends StatelessWidget {
  const HorizontalScrollbarsLayoutV3(
      {Key? key, required List<TableScrollbar> scrollbars})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.blue[300]);
  }
}
